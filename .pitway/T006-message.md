feat(M002): T006 - Transaction engine with auto-deduction

- Create transactions service with auto-deduction logic
- Create transactions routes (GET /, GET /:id, POST /)
- Update app.ts to include transactions routes
