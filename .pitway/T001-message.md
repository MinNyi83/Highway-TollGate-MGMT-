feat(M002): T001 - Auth module with JWT

- Create JWT utility with generateToken and verifyToken
- Create auth middleware for protected routes
- Create auth service with register, login, refreshToken
- Create auth routes (POST /register, /login, /refresh)
- Update app.ts to include auth routes
