feat(M002): T002 - User and Account management CRUD

- Create users service with getUserById, getUsers, updateUser
- Create users routes (GET /, GET /:id, PUT /:id)
- Create accounts service with getAccounts, getAccountById, topUpAccount
- Create accounts routes (GET /, GET /:id, POST /:id/topup)
- Update app.ts to include users and accounts routes
