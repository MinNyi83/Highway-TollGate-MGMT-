feat(M002): T003 - Vehicle management with RFID tag binding

- Create vehicles service with CRUD and RFID binding/unbinding
- Create vehicles routes (GET /, GET /:id, POST /, PUT /:id, POST /:id/rfid, DELETE /:id/rfid/:tagId)
- Update app.ts to include vehicles routes
