feat(M002): T008 - Notification system with WebSocket broadcast

- Create notifications service with CRUD and read status
- Create notifications routes (GET /, GET /:id, POST /, PUT /:id/read, PUT /read-all)
- Update app.ts to include notifications routes
