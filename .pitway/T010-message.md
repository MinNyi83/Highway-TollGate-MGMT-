feat(M002): T010 - Reporting endpoints

- Create reports service with date range queries and aggregation
- Create reports routes (GET /events, GET /transactions, GET /violations, GET /revenue, GET /violations/stats)
- Update app.ts to include reports routes
